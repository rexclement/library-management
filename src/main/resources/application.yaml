spring:
  application:
    name: LibraryManagement	
  datasource:
    url: jdbc:mysql://mysql-db:3306/librarydb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect  